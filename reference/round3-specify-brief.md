這是本產品第三輪 spec，對應 reference/Caluli_AI拍照飲食紀錄助手產品企劃書.txt 的 2.5「角色與權限」，只處理角色權限與後台管理介面，推薦餐廳的使用者端 查詢功能（由另一位開發者在 feature/round2-restaurant 分支平行進行）、 Flutter 客戶端不在本輪範圍。 第一輪（拍照辨識、熱量追蹤、LINE Login、趨勢圖表）已經完成 implement， 程式碼與資料表已實際存在於本專案中。本輪請先閱讀現有專案結構，沿用第一輪 已建立的使用者資料表、LINE Login 驗證邏輯與後端 API 架構，不要重新設計 或修改這些既有部分。 【資料表結構 — 共用契約】 店家/餐點的資料表結構請完全依照 reference/shared-schema-store-menu.md 的 定義執行，不要自行增減欄位或修改欄位名稱，因為這張表由另一位開發者的 第二輪 feature（推薦餐廳）共用，對方目前正在同一時間平行開發使用者端的 唯讀查詢功能。本輪負責這張表的寫入（新增/編輯/刪除），對方負責讀取， 雙方的分支之後會合併回同一個 main，請不要修改這份契約定義的欄位結構， 如果你認為欄位有缺漏或需要調整，請先告訴我，不要自行變更後才實作。 【本輪功能範圍】 - 角色概念：使用者新增「角色」欄位，區分一般使用者與管理員，不假設所有 使用者皆為同一種身分 - 管理員指派方式：管理員登入方式與一般使用者相同，皆採 LINE 快速登入， 不另外開放帳密登入；管理員身分由後端指定特定 LINE 使用者帳號，不是 使用者自己申請或註冊而來（實作上可能是後端資料庫裡手動標記，或提供 一個只有你自己能操作的內部指派方式，細節由 agent 提出方案） - 權限檢查層：建立僅管理員可通過的權限檢查層（例如 API 層的權限驗證 middleware/dependency），與一般使用者可呼叫的 API 完全隔開，避免一般 使用者的登入狀態誤觸後台功能 - 後台管理介面：新增／編輯／刪除店家（名稱、地址、地理座標等，欄位依 shared-schema 定義）、新增／編輯／刪除店家底下餐點（名稱、熱量、 蛋白質、碳水、脂肪，欄位依 shared-schema 定義） 【重要：介面優先度】 此後台介面服務對象是少數內部人員，體驗要求可低於主要產品，請以精簡、 快速完成為原則規劃，不需要投入視覺打磨或複雜的互動設計，例如可以用 簡單的表格 + 表單呈現，不需要對照 reference/prototype/caiuli.html 的 視覺風格，也不需要深色模式等次要體驗。 【明確排除】 - 使用者端的店家/餐點查詢、定位、距離排序（屬於第二輪，本輪不做， 本輪只負責後台寫入功能） - 一般使用者角色不需要看到任何管理員專屬的介面或選單入口 【錯誤與邊界情境，請在 spec 中一併補上驗收標準】 - 一般使用者嘗試存取管理員 API 時的行為（應回傳權限錯誤，而非顯示 一般錯誤或洩漏後台存在的線索） - 後台新增店家時，座標欄位為必填或選填？若地址與座標不一致（例如只填 地址沒填座標）時，第二輪的距離排序功能會無法排序這家店，請在 spec 中 說明此情況的處理方式（例如座標為選填，未填座標的店家在距離排序時 排在清單最後，或直接不出現在依距離排序的結果中） - 刪除店家時，若該店家底下還有餐點資料，是否一併刪除餐點（cascade delete） 或需要先刪除餐點才能刪除店家，請 spec 中明確定義 